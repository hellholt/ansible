---
- name: 'Set the MOTD.'
  ansible.builtin.get_url:
    url: "{{ sh_motd_base_url }}/{{ sh_hostname }}.txt"
    dest: '/etc/motd'
    force: no
    owner: 'root'
    mode: 0644
  ignore_errors: yes
